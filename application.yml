spring:
  datasource:
    url: jdbc:mysql://localhost:3306/CitizenDB?useSSL=false&serverTimezone=UTC
    password: root
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

